interface:
  display_name: "Resume Project Writing"
  short_description: "Turn projects into credible entries"
  default_prompt: "Use $resume-project-writing to turn these project notes into concise resume entries."
policy:
  allow_implicit_invocation: true
